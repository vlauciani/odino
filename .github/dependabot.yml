# Dependabot keeps dependencies current by opening isolated PRs (validated by CI)
# when updates or security fixes are available. https://docs.github.com/code-security/dependabot
version: 2
updates:
  # Go modules (go.mod / go.sum). Targets the develop integration branch so
  # bumps follow the git-flow path (develop -> next release -> main).
  - package-ecosystem: gomod
    directory: "/"
    target-branch: develop
    schedule:
      interval: weekly
    commit-message:
      prefix: build
    open-pull-requests-limit: 5

  # Versions of the actions used in the GitHub Actions workflows.
  - package-ecosystem: github-actions
    directory: "/"
    target-branch: develop
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    open-pull-requests-limit: 5
